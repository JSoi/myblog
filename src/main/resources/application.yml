spring:
  datasource:
    url: jdbc:mysql://localhost:3306/newschema?serverTimezone=UTC
    username: root
    password: 1104
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
      show-sql: true
  security:
    oauth2:
      client:
        clientid:
          clientSecret:
server:
  port:8080